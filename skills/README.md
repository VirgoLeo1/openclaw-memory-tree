# 🧠 Smart Memory Tree - 使用指南

## ✅ 已实现的核心功能

### 1. memory-loader.js - 记忆加载器
**功能**：
- 自动加载核心记忆（00-CORE.md, 10-INDEX.md）
- 根据关键词智能加载相关分支
- 支持递归加载（深度可配置）
- 时间/场景预加载
- 显示加载进度和统计

**用法**：
```bash
# 加载特定主题
node memory-loader.js "moltbook" 3

# 自动加载（根据时间）
node memory-loader.js
```

### 2. auto-saver.js - 自动保存器
**功能**：
- 即时保存用户指定的记忆
- 智能分类（tech/workflow/projects/preferences/platform）
- 自动版本控制（.bak 备份）
- 冲突检测
- 标签系统

**用法**：
```bash
# 保存记忆
node auto-saver.js save "今天学会了 OpenClaw 配置" "workflow" "openclaw，配置"

# 总结对话并保存
node auto-saver.js summarize "对话内容..."
```

### 3. vault-reader.js - 安全凭证读取器
**功能**：
- 安全读取 30-VAULT.md 中的凭证
- Base64/XOR 混淆加密
- 高风险操作三次确认
- 访问日志记录（不记录凭证内容）
- 用后即焚

**用法**：
```bash
# 获取凭证
node vault-reader.js get "moltbook"

# 设置凭证
node vault-reader.js set "moltbook" "your-token-here"

# 列出所有服务
node vault-reader.js list
```

### 4. heat-tracker.js - 热度追踪器
**功能**：
- 记录和更新记忆节点的热度
- 自动衰减计算（基于时间）
- 生成热度报告（每日/每周/每月）
- 火花机制：检测低热度但相关的记忆
- 自动归档建议（90 天未访问）

**用法**：
```bash
# 更新节点热度
node heat-tracker.js update "20-BRANCHES/tech/python.md"

# 生成报告
node heat-tracker.js report daily

# 检查火花
node heat-tracker.js check "python" "script"

# 列出所有节点热度
node heat-tracker.js list
```

## 📂 文件结构

```
C:\Users\33512\.openclaw\workspace\skills\smart-memory-tree\
├── SKILL.md                  # 技能文档
├── README.md                 # 本文件
├── 功能规格说明书.md          # 详细功能规格
├── memory-loader.js          # 记忆加载器
├── auto-saver.js             # 自动保存器
├── vault-reader.js           # 凭证读取器
└── heat-tracker.js           # 热度追踪器
```

## 🚀 快速开始

### 1. 首次使用
```bash
# 初始化记忆树（如果还没有）
# 记忆树位于 D:\.openclaw-backup\memory-tree\

# 测试加载核心记忆
cd "C:\Users\33512\.openclaw\workspace\skills\smart-memory-tree"
node memory-loader.js
```

### 2. 保存第一条记忆
```bash
node auto-saver.js save "今天配置了 Tavily 搜索 API" "tech" "openclaw,tavily,api"
```

### 3. 查看热度
```bash
node heat-tracker.js list
```

## 📊 配置参数

在 `99-SYSTEM\config.json` 中可配置：

```json
{
  "heat": {
    "decayFactor": 0.95,
    "heatIncrement": 10,
    "lowHeatThreshold": 40,
    "highHeatThreshold": 80,
    "halfLifeHours": 168,
    "archiveAfterDays": 90
  },
  "autoLoad": {
    "defaultDepth": 3,
    "maxDepth": 5
  }
}
```

## 🔒 安全提示

1. **凭证保护**：30-VAULT.md 中的凭证已加密，不要分享给他人
2. **定期备份**：建议定期备份整个记忆树目录
3. **热度维护**：定期运行热度报告，归档旧记忆

## 📝 下一步

待实现的功能（阶段 2+）：
- [ ] 搜索引擎（全文搜索）
- [ ] 对话历史自动保存
- [ ] 定期任务调度
- [ ] 知识图谱可视化
- [ ] 云同步

---

**创建时间**: 2026-02-24  
**版本**: 2.0.0  
**状态**: 核心功能已实现
